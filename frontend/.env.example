# ── API base URL ─────────────────────────────────────────────
# In development, vite proxy handles /api → localhost:8000
# In production, set to your backend URL if not using nginx proxy
REACT_APP_API_URL=/api
